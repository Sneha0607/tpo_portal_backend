const User = require("../models/User");
const CompanyForm = require("../models/CompanyForm");


// GET names and count of all companies with package

// GET avg of each course, branch with highest lowest package 

// GET % placement